<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CS2itemViewer.MainPage"
             Title = "CS2 Item Viewer">



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Label above ScrollView -->
        <Entry
            Placeholder="Search..."
            Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="1"
            HorizontalOptions="FillAndExpand"
            VerticalOptions="Center">
        </Entry>

        <!-- ScrollView -->
        <ScrollView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4">
            <VerticalStackLayout>
                <Image
                    Source="dotnet_bot.png"
                    HeightRequest="185"
                    Aspect="AspectFit"
                    SemanticProperties.Description="dot net bot in a race car number eight" />

                <Label
                    Text="Hello, World!"
                    Style="{StaticResource Headline}"
                    SemanticProperties.HeadingLevel="Level1" />

                <Label
                    Text="Welcome to &#10;.NET Multi-platform App UI"
                    Style="{StaticResource SubHeadline}"
                    SemanticProperties.HeadingLevel="Level2"
                    SemanticProperties.Description="Welcome to dot net Multi platform App U I" />
            </VerticalStackLayout>
        </ScrollView>

        <!-- Buttons -->
        <Button Text="Filter" Grid.Row="0" Grid.Column="1" HorizontalOptions="Center" />
        <Button Text="+" Grid.Row="0" Grid.Column="2" HorizontalOptions="Center" />
        <Button Text="login" Grid.Row="0" Grid.Column="3" HorizontalOptions="Center" />
    </Grid>

</ContentPage>
