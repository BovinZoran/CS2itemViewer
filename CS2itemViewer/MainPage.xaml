<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CS2itemViewer.MainPage"
             xmlns:model="clr-namespace:CS2itemViewer.Model">

    <ScrollView>
        <StackLayout>
            <CollectionView>
                <CollectionView.ItemsSource>
                    <x:Array Type="{x:Type model:Skin}">
                        <model:Skin
                            MarketName="Baboon"
                            Image="Resources/Images/placeholderpicture.jpg"
                            Name="Skin Name"
                            DescriptionFloat="hallo dit is test"
                            DescriptionText="..."
                            PriceLatestSell="00.00"/>
                        <model:Skin
                            MarketName="Baboon"
                            Image="Resources/Images/placeholderpicture.jpg"
                            Name="Skin Name"
                            DescriptionFloat="..."
                            DescriptionText="..."
                            PriceLatestSell="00.00"/>
                        <model:Skin
                            MarketName="Baboon"
                            Image="Resources/Images/placeholderpicture.jpg"
                            Name="Skin Name"
                            DescriptionFloat="..."
                            DescriptionText="..."
                            PriceLatestSell="00.00"/>
                    </x:Array>
                </CollectionView.ItemsSource>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:Skin">
                        <HorizontalStackLayout Padding="10">
                            <Image Aspect="AspectFill" HeightRequest="100" Source="{Binding Image}" WidthRequest="100" />
                            <Label VerticalOptions="Center" TextColor="Gray">
                                <Label.Text>
                                    <MultiBinding StringFormat="{}{0} | {1}">
                                        <Binding Path="Name" />
                                        <Binding Path="Location" />
                                    </MultiBinding>
                                </Label.Text>
                            </Label>
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <HorizontalStackLayout Padding="10">
                <Image Aspect="AspectFill" HeightRequest="100" Source="{Binding Image}" WidthRequest="100" />
                <VerticalStackLayout VerticalOptions="Center">
                    <Label Text="{Binding Name}" FontSize="24" TextColor="Gray"/>
                    <Label Text="{Binding Location}" FontSize="18" TextColor="Gray"/>
                </VerticalStackLayout>
            </HorizontalStackLayout>
        </StackLayout>
    </ScrollView>

</ContentPage>